name: Deploy

on:
  workflow_run:
    workflows: ["Test"]
    types: [completed]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy application
        run: |
          echo "================================"
          echo " Déploiement JavaCalculator 🚀"
          echo " Succès"
          echo "================================"
